import { AddSongForm, SongCardList } from "@/features/song-manager";

const SongPage = () => {
  return (
    <div>
      <SongCardList />
      <AddSongForm />
    </div>
  );
};

export default SongPage;
